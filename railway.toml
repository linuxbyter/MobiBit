[build]
buildCommand = "composer install --ignore-platform-reqs --no-scripts && npm install && npm run build"

[deploy]
startCommand = "php artisan config:cache && php artisan serve --host=0.0.0.0 --port=$PORT"
