[build]
buildCommand = "composer install --ignore-platform-reqs --no-scripts && npm install && npm run build && php artisan config:cache"

[deploy]
startCommand = "php artisan serve --host=0.0.0.0 --port=$PORT --env=production"
